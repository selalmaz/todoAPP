import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  tabBar: {
    backgroundColor: '#FFFFFF',
    elevation: 10,
  },
  tabBarLabel: {
    fontSize: 14,
    fontWeight: '600',
  },
});
