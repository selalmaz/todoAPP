import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  logoContainer: {
    paddingTop: 25,
    justifyContent: 'center',
    alignItems: 'center',
    marginBottom: 20,
  },

  inputContainer: {
    marginBottom: 20,
  },
});
